# Interview Ace

Project scaffold:

- `backend/` for FastAPI API routes, agents, RAG, models, and core utilities.
- `frontend/` for app UI source and Electron packaging.
- `scripts/` for environment/bootstrap scripts.
- `docs/` for design and architecture docs.
